<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navStreams">

    <fragment
        android:id="@+id/navStreams"
        android:name="ru.s44khin.messenger.ui.main.streams.StreamsFragment"
        android:label="@string/streams"
        tools:layout="@layout/fragment_streams" />

    <fragment
        android:id="@+id/navPeople"
        android:name="ru.s44khin.messenger.ui.main.members.MembersFragment"
        android:label="@string/people"
        tools:layout="@layout/fragment_people" />

    <fragment
        android:id="@+id/navProfile"
        android:name="ru.s44khin.messenger.ui.main.profile.ProfileFragment"
        android:label="@string/profile"
        tools:layout="@layout/fragment_profile" />
</navigation>